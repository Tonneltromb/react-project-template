import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <div id='spinner'>
            <div className="loader">Loading</div>
        </div>
    );
};

export default Spinner;